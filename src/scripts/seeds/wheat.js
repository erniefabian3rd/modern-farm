/* create a function that has one parameter, seed
    Define an object with three properties: type, height, output
    Check chart for correct data inputs
    return object
*/

export const createWheat = () => {
    const wheatObject = {
        type: "Wheat",
        height: 230,
        output: 6
    }
    
    return wheatObject
}