/* create a function that has one parameter, seed
    Define an object with three properties: type, height, output
    Check chart for correct data inputs
    return object

    this function should return two identical objects
*/

export const createCorn = () => {
    const cornObject = [
    {
        type: "Corn",
        height: 180,
        output: 6
    },
    {
        type: "Corn",
        height: 180,
        output: 6
    }
]
    return cornObject
}